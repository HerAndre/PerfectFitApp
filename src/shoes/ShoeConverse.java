package shoes;

import size.Size;

/**
 * Extended class which passes specific params to the Shoe super class.
 */
public class ShoeConverse extends Shoe {

    public ShoeConverse(Size size) {
        super("Converse", size);
    }
}
