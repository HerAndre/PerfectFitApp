package shoes;

import size.Size;

/**
 * Extended class which passes specific params to the Shoe super class.
 */
public class ShoePuma extends Shoe {

    public ShoePuma(Size size) {
        super("Puma", size);
    }
}
