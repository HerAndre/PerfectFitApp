package shoes;

import size.Size;

/**
 * Extended class which passes specific params to the Shoe super class.
 */
public class ShoeReebok extends Shoe {

    public ShoeReebok(Size size) {
        super("Reebok", size);
    }
}
